#include "main.h"

/**
 * _isupper - uppercase or not
 * @a: checked parameter
 * Return: 1 if uppercase, 0 if not
 */

int _isupper(int a)
{
	if (a >= 65 && a <= 90)
	{
	return (1);
	}
	return (0);
}
